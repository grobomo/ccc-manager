apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ccc-manager.fullname" . }}-config
  labels:
    {{- include "ccc-manager.labels" . | nindent 4 }}
data:
  project.yaml: |
    {{- .Values.projectConfig | nindent 4 }}
