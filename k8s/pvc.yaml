apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ccc-manager-state
  labels:
    app: ccc-manager
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  # storageClassName: gp3  # Uncomment for AWS EBS, or use your cluster's default
