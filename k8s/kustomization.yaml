apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - servicemonitor.yaml
  - pvc.yaml

commonLabels:
  app.kubernetes.io/name: ccc-manager
  app.kubernetes.io/part-of: ccc

namespace: default
